---
title: chat history with ai
---

a prompt for summarize:

```md
**按以下要求总结本次对话的全部内容：**

1. **回顾整个对话**，按主题或话题分类（若只有一个主题，则只用一个二级标题）。
2. **输出格式要求**（请严格遵循）：
   - 每个主题使用一个**二级标题**（`## `）作为标题，标题简明扼要概括该主题。
   - 在该二级标题下方，**第一行**用 **引用语法**（`> `）概括我（用户）在该主题下提出的核心问题或需求。
   - 紧接着，用若干个**三级标题**（`### `）分要点列出您（模型）回答中的关键信息、步骤或结论。每个三级标题对应一个要点，标题后跟简要说明（可含短段落或列表）。
3. **层级控制**：最多使用四级标题，不再使用更深的标题层级；避免冗余修饰，保持干净。
4. **完整性与简洁性**：覆盖所有重要对话节点，但表述精炼，不遗漏关键事实、建议或结论。
5. 最终输出以纯文本 Markdown 形式呈现，方便我直接保存为 `.md` 文件。
```
